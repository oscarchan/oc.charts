<%= form_for(@stock_chart) do |f| %>
  <% if @stock_chart.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stock_chart.errors.count, "error") %> prohibited this stock_chart from being saved:</h2>

      <ul>
      <% @stock_chart.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
